/* ===================================================================
 KUBERNETES ARCHITECTURE - Level 13: Cloud-Native Engineering
 Control plane, worker nodes, cluster communication, etcd consensus,
 admission controllers, API aggregation, HA setup, kubectl lifecycle.
 =================================================================== */
'use strict';
PageData.register('k8s-architecture', {
 title: 'Kubernetes Architecture',
 level: 13,
 group: 'cloud-native',
 description: 'Deep dive into Kubernetes control plane, worker node components, cluster communication, and high-availability patterns.',
 difficulty: 'advanced',
 estimatedMinutes: 60,
 prerequisites: ['cloud-basics', 'docker-deep-dive'],
 sections: [
 {
 title: 'Introduction',
 content: '<p><strong>Kubernetes (K8s)</strong> is an open-source container orchestration platform that automates deployment, scaling, and management of containerized applications. Originally designed by Google and now maintained by the CNCF, it has become the de facto standard for running production workloads at scale.</p><p>Understanding Kubernetes architecture is essential because:</p><ul><li>It underpins virtually every modern cloud-native deployment</li><li>Interview questions at senior+ levels expect deep knowledge of internals</li><li>Debugging production issues requires understanding how components interact</li><li>Designing HA clusters demands knowledge of failure domains and consensus</li></ul><p>This module covers the full architecture from API server request handling through etcd persistence to kubelet pod execution on worker nodes.</p>'
 },
 {
 title: 'Control Plane Components',
 content: '<p>The <strong>control plane</strong> maintains the desired state of the cluster.</p><h4>kube-apiserver</h4><p>The front door to the cluster. Every interaction goes through the API server. It validates requests, performs authentication/authorization (RBAC), runs admission controllers, and persists state to etcd. It is the ONLY component that talks directly to etcd.</p><h4>etcd</h4><p>A distributed consistent key-value store holding ALL cluster state. Uses Raft consensus for data consistency across replicas.</p><h4>kube-scheduler</h4><p>Watches for newly created Pods with no assigned node. Evaluates constraints (resource requests, affinity/anti-affinity, taints/tolerations, topology spread) and selects the optimal node.</p><h4>kube-controller-manager</h4><p>Runs control loops that watch cluster state and move current state toward desired state. Key controllers: ReplicaSet, Deployment, Node, Job, EndpointSlice, ServiceAccount.</p><h4>cloud-controller-manager</h4><p>Integrates with cloud provider APIs. Manages load balancers, routes, node lifecycle, and volume provisioning.</p>',
 mermaid: 'graph LR; kubectl[kubectl] -->|HTTPS| API[kube-apiserver]; API -->|read/write| ETCD[(etcd)]; SCHED[kube-scheduler] -->|watch| API; CM[controller-manager] -->|watch/update| API; CCM[cloud-controller-mgr] -->|cloud APIs| CLOUD[AWS/GCP/Azure]'
 },
 {
 title: 'Worker Node Components',
 content: '<p>Worker nodes run containerized application workloads. Each node has critical agents:</p><h4>kubelet</h4><p>Primary node agent. Registers the node with the API server, watches for PodSpecs, ensures containers are running and healthy. Communicates with the container runtime via CRI (Container Runtime Interface).</p><h4>kube-proxy</h4><p>Network proxy implementing the Service concept. Maintains iptables or IPVS rules for traffic routing to backend Pods. Handles ClusterIP, NodePort, and LoadBalancer traffic.</p><h4>Container Runtime</h4><p>Runs containers. Supports any CRI-compliant runtime: containerd (most common), CRI-O (OpenShift), or Docker via cri-dockerd shim.</p><h4>Optional Components</h4><ul><li><strong>CNI plugin:</strong> Pod networking (Calico, Cilium, Flannel)</li><li><strong>CSI drivers:</strong> Storage provisioning</li><li><strong>Node Problem Detector:</strong> Hardware/kernel monitoring</li></ul>',
 mermaid: 'graph TB; subgraph Worker Node; KUBELET[kubelet] -->|CRI gRPC| RUNTIME[containerd]; KUBELET -->|health checks| PODS[Running Pods]; KPROXY[kube-proxy] -->|iptables/IPVS| PODS; CNI[CNI Plugin] -->|network| PODS; end; KUBELET -->|status| API[kube-apiserver]; KPROXY -->|watch Services| API'
 },
 {
 title: 'kubectl Request Lifecycle',
 content: '<p>When you run <code>kubectl apply -f deployment.yaml</code>, the request follows this complete path:</p><ol><li><strong>Client-side validation:</strong> kubectl validates YAML syntax and schema</li><li><strong>HTTP request:</strong> POST/PUT sent to API server endpoint via HTTPS</li><li><strong>Authentication:</strong> Verified via client certificates, bearer tokens, or OIDC</li><li><strong>Authorization:</strong> RBAC checks permission for verb+resource+namespace</li><li><strong>Mutating Admission:</strong> Webhooks can modify the object (inject sidecars, add labels)</li><li><strong>Validating Admission:</strong> Webhooks can reject (enforce policies)</li><li><strong>Schema Validation:</strong> Object validated against OpenAPI schema</li><li><strong>Persistence:</strong> Serialized and written to etcd</li><li><strong>Controller Reaction:</strong> Deployment controller detects new object, creates ReplicaSet</li><li><strong>Scheduler Reaction:</strong> Scheduler assigns unscheduled Pods to nodes</li><li><strong>Kubelet Reaction:</strong> Kubelet pulls images, starts containers via CRI</li></ol><p>The entire flow from kubectl to running container typically takes 2-10 seconds.</p>'
 },
 {
 title: 'etcd and Raft Consensus',
 content: '<p><strong>etcd</strong> is the single source of truth for the entire cluster.</p><h4>Raft Consensus</h4><p>etcd uses Raft to maintain consistency. A cluster of N nodes tolerates (N-1)/2 failures. 3-node cluster tolerates 1 failure; 5-node tolerates 2.</p><h4>Key Properties</h4><ul><li><strong>Linearizable reads:</strong> Most recent write always returned (default)</li><li><strong>Watch API:</strong> Streaming gRPC for change notifications</li><li><strong>Compaction:</strong> Periodic history cleanup prevents unbounded growth</li><li><strong>Snapshots:</strong> Enable faster recovery without full WAL replay</li></ul><h4>Production Best Practices</h4><ul><li>Always run 3 or 5 members (never even numbers)</li><li>Use SSD/NVMe storage (latency-sensitive)</li><li>Dedicated nodes (no workload co-location)</li><li>Monitor disk fsync duration (over 10ms = problems)</li><li>Automated backups with etcdctl snapshot save</li></ul>',
 code: '# etcd operations\netcdctl endpoint health --cluster\netcdctl snapshot save /backup/etcd.db\netcdctl member list -w table\netcdctl defrag --endpoints=https://127.0.0.1:2379',
 language: 'bash'
 },
 {
 title: 'Admission Controllers and API Aggregation',
 content: '<p><strong>Admission controllers</strong> intercept API server requests AFTER auth but BEFORE persistence to etcd.</p><h4>Mutating Admission Webhooks</h4><p>Can modify incoming objects. Run first. Examples: Istio sidecar injection, default resource limits, image registry rewriting.</p><h4>Validating Admission Webhooks</h4><p>Accept or reject only. Run after mutating. Examples: enforce label requirements, block privileged containers.</p><h4>Built-in Controllers</h4><ul><li><strong>NamespaceLifecycle:</strong> Prevents objects in terminating namespaces</li><li><strong>LimitRanger:</strong> Applies default resource limits</li><li><strong>ServiceAccount:</strong> Automounts SA tokens</li><li><strong>ResourceQuota:</strong> Enforces namespace quotas</li><li><strong>PodSecurity:</strong> Enforces Pod Security Standards</li></ul><h4>API Aggregation Layer</h4><p>Extends the Kubernetes API with custom API servers. APIService objects register custom backends that the main API server proxies to transparently.</p><h4>Policy Engines</h4><ul><li><strong>OPA/Gatekeeper:</strong> Rego-based policies</li><li><strong>Kyverno:</strong> YAML-native policies</li><li><strong>Kubewarden:</strong> WebAssembly-based policies</li></ul>',
 code: '# Kyverno policy - require labels on Pods\napiVersion: kyverno.io/v1\nkind: ClusterPolicy\nmetadata:\n name: require-labels\nspec:\n validationFailureAction: Enforce\n rules:\n - name: check-team-label\n match:\n any:\n - resources:\n kinds: [Pod]\n validate:\n pattern:\n metadata:\n labels:\n team: "*"',
 language: 'yaml'
 },
 {
 title: 'High Availability Control Plane',
 content: '<p>Production clusters require HA control planes to eliminate single points of failure.</p><h4>Multi-Master Architecture</h4><ul><li><strong>API Server:</strong> Stateless - run 3+ replicas behind a load balancer (active-active).</li><li><strong>etcd:</strong> Run 3 or 5 nodes. Stacked (co-located) or external (dedicated nodes).</li><li><strong>Scheduler/Controller Manager:</strong> Leader election via leases. Only one active instance; others on standby.</li></ul><h4>Stacked vs External etcd</h4><p><strong>Stacked:</strong> Simpler setup, fewer machines. Risk: losing a node loses both control plane and etcd member.</p><p><strong>External:</strong> Better fault isolation, independent scaling. Recommended for large production clusters.</p><h4>Failure Scenarios</h4><ul><li>1 of 3 API servers down: No impact (LB routes elsewhere)</li><li>1 of 3 etcd nodes down: Cluster continues (quorum with 2/3)</li><li>2 of 3 etcd down: Read-only (no quorum for writes)</li><li>All API servers down: Existing Pods run, no new changes possible</li></ul>',
 mermaid: 'graph TB; LB[Load Balancer] --> API1[API Server 1]; LB --> API2[API Server 2]; LB --> API3[API Server 3]; API1 --> ETCD1[(etcd 1)]; API2 --> ETCD2[(etcd 2)]; API3 --> ETCD3[(etcd 3)]; ETCD1 --- ETCD2; ETCD2 --- ETCD3; ETCD1 --- ETCD3'
 },
 {
 title: 'Cluster Communication and Security',
 content: '<p>All communication is secured by TLS and controlled by RBAC.</p><h4>Communication Paths</h4><ul><li><strong>kubectl to API Server:</strong> HTTPS with client certificate or bearer token</li><li><strong>API Server to etcd:</strong> Mutual TLS with dedicated CA</li><li><strong>API Server to Kubelet:</strong> HTTPS for log streaming, exec, port-forward</li><li><strong>Kubelet to API Server:</strong> TLS with bootstrap tokens or client certificates</li><li><strong>Pod to API Server:</strong> Service account tokens (projected volumes)</li></ul><h4>RBAC</h4><p>Four objects: Role, ClusterRole, RoleBinding, ClusterRoleBinding. Permissions are additive only (no deny rules). Service accounts get minimal permissions by default.</p>'
 },
 {
 title: 'Common Mistakes',
 content: '<p>Architecture-level mistakes that cause production outages:</p><ul><li><strong>Single etcd node:</strong> Losing it means losing entire cluster state</li><li><strong>Even number of etcd nodes:</strong> No benefit over odd (same failure tolerance, more overhead)</li><li><strong>No resource limits on system components:</strong> App pods can starve kubelet/kube-proxy</li><li><strong>etcd on slow disks:</strong> Causes cluster-wide slowness and leader elections</li><li><strong>No API server audit logging:</strong> Security incidents become uninvestigable</li><li><strong>Exposing API server publicly:</strong> Should be behind VPN or bastion host</li><li><strong>Skipping admission controllers:</strong> A single namespace can consume all resources</li><li><strong>Not monitoring certificate expiry:</strong> Expired certs cause total communication failure</li></ul>',
 callout: { type: 'warning', title: 'Critical', text: 'Always test etcd backup restoration procedures regularly. An untested backup is not a backup.' }
 },
 {
 title: 'Interview Tips',
 content: '<p>What interviewers look for with K8s architecture questions:</p><ul><li><strong>Component knowledge:</strong> Name all control plane and node components with roles</li><li><strong>Communication flow:</strong> Trace a request from kubectl to running pod</li><li><strong>Failure reasoning:</strong> What happens when specific components fail?</li><li><strong>HA design:</strong> How to design a production-grade cluster</li><li><strong>etcd understanding:</strong> Why critical? How does consensus work?</li></ul>',
 callout: { type: 'tip', title: 'Pro Tip', text: 'Draw the architecture diagram on a whiteboard. Start with API server in center, etcd behind it, workers below. This demonstrates clear understanding.' }
 }
 ],
 questions: [
 { id: 'k8s-arch-q1', level: 'junior', title: 'What are the main components of the Kubernetes control plane?', answer: '<p>The control plane consists of: <strong>kube-apiserver</strong> (API gateway), <strong>etcd</strong> (state store), <strong>kube-scheduler</strong> (pod placement), <strong>kube-controller-manager</strong> (reconciliation loops), and <strong>cloud-controller-manager</strong> (cloud provider integration).</p>' },
 { id: 'k8s-arch-q2', level: 'junior', title: 'What is the role of kubelet on a worker node?', answer: '<p>Kubelet is the primary agent on each worker node. It registers the node with the API server, watches for Pod assignments, ensures containers are running and healthy, reports status back to the control plane, and communicates with the container runtime via CRI.</p>' },
 { id: 'k8s-arch-q3', level: 'mid', title: 'What happens when the kube-scheduler goes down?', answer: '<p>Existing Pods continue running unaffected. New Pods remain in Pending state until scheduler recovers. In HA setups, a standby scheduler takes over via leader election within seconds.</p>' },
 { id: 'k8s-arch-q4', level: 'mid', title: 'Explain the difference between kube-proxy iptables mode and IPVS mode.', answer: '<p><strong>iptables:</strong> One rule per endpoint. O(n) lookup. Degrades with thousands of services. Random balancing only. <strong>IPVS:</strong> Kernel-level hash tables. O(1) lookup. Multiple algorithms (round-robin, least-connections). Recommended for 1000+ services.</p>' },
 { id: 'k8s-arch-q5', level: 'mid', title: 'Why should you never run an even number of etcd nodes?', answer: '<p>etcd requires quorum (majority) for writes. With 2 nodes, losing 1 loses quorum. With 4 nodes, you can only lose 1 (need 3/4) - same tolerance as 3 nodes but more overhead. Odd numbers (3 or 5) maximize fault tolerance per node.</p>' },
 { id: 'k8s-arch-q6', level: 'senior', title: 'Trace the full lifecycle of a kubectl apply request.', answer: '<p>1) Client validates YAML. 2) HTTPS POST to API server. 3) Authentication (certs/tokens). 4) RBAC authorization. 5) Mutating admission webhooks. 6) Validating admission webhooks. 7) Schema validation. 8) Persist to etcd. 9) Deployment controller creates ReplicaSet. 10) ReplicaSet creates Pods. 11) Scheduler assigns to node. 12) Kubelet pulls images, starts containers. 13) Readiness probe passes, Endpoints updated.</p>' },
 { id: 'k8s-arch-q7', level: 'senior', title: 'How do admission controllers differ from RBAC?', answer: '<p><strong>RBAC</strong> answers: Can this user perform this action? Identity-based access control. <strong>Admission controllers</strong> answer: Should this specific object be allowed? They inspect object content regardless of who submitted it. RBAC for permissions, admission for policy enforcement.</p>' },
 { id: 'k8s-arch-q8', level: 'senior', title: 'What is the API aggregation layer and when would you use it?', answer: '<p>Extends the Kubernetes API by registering custom API servers behind kube-apiserver. APIService objects proxy requests to your backend. Use cases: metrics-server (metrics.k8s.io), custom resources needing custom storage or complex validation. Differs from CRDs which use the main API server and etcd.</p>' },
 { id: 'k8s-arch-q9', level: 'lead', title: 'Design a highly available Kubernetes control plane for a mission-critical application.', answer: '<p>Key decisions: 1) 3-5 control plane nodes across AZs. 2) External etcd on dedicated SSD nodes. 3) TCP load balancer with /healthz checks. 4) Automated etcd snapshots every 30min with tested restore. 5) Monitoring: etcd leader changes, API server latency, cert expiry. 6) Anti-affinity rules across failure domains. 7) Node auto-repair for failed control plane nodes.</p>' },
 { id: 'k8s-arch-q10', level: 'lead', title: 'How would you debug etcd high latency affecting the entire cluster?', answer: '<p>1) Check etcd_disk_wal_fsync_duration_seconds - over 10ms means disk bottleneck, move to SSD. 2) Check etcd_network_peer_round_trip_time - network issues between members. 3) Check DB size - approaching 2GB quota needs compaction+defrag. 4) Review watch connections count. 5) Check API server request rate and throttle if needed. 6) Consider splitting into smaller clusters.</p>' },
 { id: 'k8s-arch-q11', level: 'architect', title: 'Compare stacked vs external etcd topology with operational implications.', answer: '<p><strong>Stacked:</strong> etcd on same nodes as API server. Fewer machines, simpler. Risk: losing node loses both. Good for smaller clusters. <strong>External:</strong> Dedicated etcd nodes. Better isolation, independent scaling, optimized hardware (NVMe). More machines, complex setup, separate maintenance. Doubles certificate management complexity. Choose external for regulated environments and large-scale production.</p>' },
 { id: 'k8s-arch-q12', level: 'architect', title: 'How would you architect a multi-cluster Kubernetes platform for a global organization?', answer: '<p>1) Fleet management with GitOps (ArgoCD/Flux). 2) Regional clusters for data sovereignty and latency. 3) Multi-cluster service mesh (Istio) for cross-cluster discovery. 4) DNS-based global load balancing. 5) CockroachDB/Spanner for globally consistent state. 6) Centralized observability (Thanos for multi-cluster Prometheus). 7) Shared PKI with federated identity (OIDC). 8) Policy-as-code consistently enforced across all clusters.</p>' }
 ]
});
