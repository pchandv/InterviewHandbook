/* ===================================================================
 KUBERNETES WORKLOADS - Level 13: Cloud-Native Engineering
 Pods, ReplicaSets, Deployments, StatefulSets, DaemonSets, Jobs,
 CronJobs, Pod lifecycle, resource management, QoS classes.
 =================================================================== */
'use strict';
PageData.register('k8s-workloads', {
 title: 'Kubernetes Workloads',
 level: 13,
 group: 'cloud-native',
 description: 'Master Kubernetes workload resources: Pods, Deployments, StatefulSets, DaemonSets, Jobs, and resource management patterns.',
 difficulty: 'advanced',
 estimatedMinutes: 55,
 prerequisites: ['k8s-architecture', 'docker-deep-dive'],
 sections: [
 {
 title: 'Introduction',
 content: '<p><strong>Workloads</strong> are the applications running on Kubernetes. The platform provides several resource types (controllers) that manage Pod lifecycle differently depending on application requirements.</p><p>This module covers:</p><ul><li>Pods and multi-container patterns (sidecar, init, ambassador)</li><li>ReplicaSets and Deployments (rolling updates, rollbacks)</li><li>StatefulSets for databases and stateful applications</li><li>DaemonSets for node-level services</li><li>Jobs and CronJobs for batch processing</li><li>Pod lifecycle phases and conditions</li><li>Resource requests, limits, and QoS classes</li></ul><p>Understanding workload types is critical for choosing the right abstraction for each application component.</p>'
 },
 {
 title: 'Pods and Multi-Container Patterns',
 content: '<p>A <strong>Pod</strong> is the smallest deployable unit in Kubernetes - a group of one or more containers with shared storage, network (same IP), and lifecycle.</p><h4>Multi-Container Patterns</h4><ul><li><strong>Sidecar:</strong> Helper container alongside main container. Examples: log shippers, service mesh proxies (Envoy), config reloaders. Shares volumes and network with main container.</li><li><strong>Init Containers:</strong> Run to completion BEFORE main containers start. Use for: schema migrations, config generation, waiting for dependencies. Run sequentially in order.</li><li><strong>Ambassador:</strong> Proxy container that simplifies access to external services. Main container connects to localhost; ambassador handles routing to actual service.</li><li><strong>Adapter:</strong> Transforms output from main container into a standard format. Example: converting logs to a common format for aggregation.</li></ul><h4>Pod Networking</h4><p>All containers in a Pod share the same network namespace (same IP). They communicate via localhost. Different ports distinguish containers.</p>',
 mermaid: 'graph LR; subgraph Pod; MAIN[Main Container :8080] --- SIDE[Sidecar - Envoy :15001]; MAIN --- LOGS[Log Shipper]; INIT[Init Container] -.->|runs first| MAIN; end; MAIN -->|localhost:15001| SIDE; SIDE -->|mTLS| MESH[Service Mesh]'
 },
 {
 title: 'ReplicaSets and Deployments',
 content: '<p><strong>ReplicaSet</strong> ensures a specified number of Pod replicas are running at all times. If a Pod dies, it creates a new one. You rarely create ReplicaSets directly.</p><p><strong>Deployment</strong> manages ReplicaSets and provides declarative updates. It is the standard workload for stateless applications.</p><h4>Rolling Update Strategy</h4><p>Default strategy. Creates new ReplicaSet, gradually scales it up while scaling old one down. Parameters:</p><ul><li><strong>maxSurge:</strong> Max extra Pods during update (default 25%). Controls how fast new version rolls out.</li><li><strong>maxUnavailable:</strong> Max Pods that can be unavailable (default 25%). Ensures capacity during update.</li></ul><h4>Recreate Strategy</h4><p>Kills all existing Pods before creating new ones. Use when application cannot run multiple versions simultaneously (shared database schema, exclusive locks).</p><h4>Rollback</h4><p>Deployments maintain revision history. Rollback instantly reverts to previous ReplicaSet. <code>kubectl rollout undo</code> triggers immediate rollback. <code>kubectl rollout history</code> shows all revisions.</p>',
 code: 'apiVersion: apps/v1\nkind: Deployment\nmetadata:\n name: web-app\nspec:\n replicas: 3\n strategy:\n type: RollingUpdate\n rollingUpdate:\n maxSurge: 1\n maxUnavailable: 0\n selector:\n matchLabels:\n app: web\n template:\n metadata:\n labels:\n app: web\n spec:\n containers:\n - name: web\n image: myapp:v2\n ports:\n - containerPort: 8080\n readinessProbe:\n httpGet:\n path: /health\n port: 8080\n initialDelaySeconds: 5',
 language: 'yaml'
 },
 {
 title: 'StatefulSets',
 content: '<p><strong>StatefulSets</strong> manage stateful applications that need stable identities, ordered deployment, and persistent storage.</p><h4>Guarantees</h4><ul><li><strong>Stable network identity:</strong> Pods get predictable DNS names (web-0, web-1, web-2) that persist across rescheduling</li><li><strong>Ordered deployment:</strong> Pods are created sequentially (0, 1, 2). Each must be Running+Ready before next starts.</li><li><strong>Ordered termination:</strong> Deleted in reverse order (2, 1, 0)</li><li><strong>Stable storage:</strong> Each Pod gets its own PersistentVolumeClaim that follows it across rescheduling</li></ul><h4>Use Cases</h4><ul><li>Databases (PostgreSQL, MySQL, MongoDB replicas)</li><li>Message queues (Kafka, RabbitMQ clusters)</li><li>Distributed systems requiring leader election (ZooKeeper, etcd)</li><li>Any application needing stable hostname for peer discovery</li></ul><h4>Headless Service Requirement</h4><p>StatefulSets require a headless Service (clusterIP: None) for DNS-based peer discovery. Each Pod gets a DNS record: <code>pod-name.service-name.namespace.svc.cluster.local</code></p>',
 code: 'apiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n name: postgres\nspec:\n serviceName: postgres-headless\n replicas: 3\n selector:\n matchLabels:\n app: postgres\n template:\n metadata:\n labels:\n app: postgres\n spec:\n containers:\n - name: postgres\n image: postgres:15\n ports:\n - containerPort: 5432\n volumeMounts:\n - name: data\n mountPath: /var/lib/postgresql/data\n volumeClaimTemplates:\n - metadata:\n name: data\n spec:\n accessModes: [ReadWriteOnce]\n resources:\n requests:\n storage: 10Gi',
 language: 'yaml'
 },
 {
 title: 'DaemonSets',
 content: '<p><strong>DaemonSets</strong> ensure a copy of a Pod runs on every node (or a subset via nodeSelector/tolerations).</p><h4>Common Use Cases</h4><ul><li><strong>Log collection:</strong> Fluentd, Filebeat on every node</li><li><strong>Node monitoring:</strong> Prometheus Node Exporter, Datadog agent</li><li><strong>Network plugins:</strong> Calico, Cilium CNI agents</li><li><strong>Storage daemons:</strong> GlusterFS, Ceph agents</li><li><strong>Security agents:</strong> Falco, Twistlock runtime security</li></ul><h4>Update Strategies</h4><ul><li><strong>RollingUpdate (default):</strong> One node at a time</li><li><strong>OnDelete:</strong> Only updates on manual Pod deletion</li></ul><p>DaemonSet Pods tolerate node taints automatically and start before regular Pods on new nodes.</p>'
 },
 {
 title: 'Jobs and CronJobs',
 content: '<p><strong>Jobs</strong> run Pods to completion. Useful for batch processing, migrations, and one-time tasks.</p><h4>Job Patterns</h4><ul><li><strong>Single completion:</strong> One Pod runs to success</li><li><strong>Parallel fixed count:</strong> N Pods all must complete</li><li><strong>Work queue:</strong> Multiple Pods process queue items</li></ul><h4>Failure Handling</h4><ul><li><strong>backoffLimit:</strong> Max retries (default 6)</li><li><strong>activeDeadlineSeconds:</strong> Hard timeout</li><li><strong>ttlSecondsAfterFinished:</strong> Auto-cleanup</li></ul><h4>CronJobs</h4><p>Creates Jobs on a schedule (cron syntax). Key: concurrencyPolicy (Allow/Forbid/Replace), startingDeadlineSeconds.</p>',
 code: 'apiVersion: batch/v1\nkind: CronJob\nmetadata:\n name: db-backup\nspec:\n schedule: "0 2 * * *"\n concurrencyPolicy: Forbid\n successfulJobsHistoryLimit: 3\n jobTemplate:\n spec:\n template:\n spec:\n containers:\n - name: backup\n image: postgres:15\n command: [pg_dumpall]\n restartPolicy: OnFailure\n backoffLimit: 3',
 language: 'yaml'
 },
 {
 title: 'Pod Lifecycle',
 content: '<p>Understanding Pod lifecycle is essential for debugging and designing resilient applications.</p><h4>Pod Phases</h4><ul><li><strong>Pending:</strong> Pod accepted but containers not yet running. Reasons: scheduling, image pull, init containers.</li><li><strong>Running:</strong> At least one container is running or starting/restarting.</li><li><strong>Succeeded:</strong> All containers exited with code 0 (Jobs).</li><li><strong>Failed:</strong> All containers terminated, at least one exited non-zero.</li><li><strong>Unknown:</strong> Cannot determine state (node communication failure).</li></ul><h4>Container States</h4><ul><li><strong>Waiting:</strong> Not yet running (pulling image, starting)</li><li><strong>Running:</strong> Executing without issues</li><li><strong>Terminated:</strong> Completed execution (success or failure)</li></ul><h4>Probes</h4><ul><li><strong>Liveness probe:</strong> Is the container alive? Failure triggers restart.</li><li><strong>Readiness probe:</strong> Can it serve traffic? Failure removes from Service endpoints.</li><li><strong>Startup probe:</strong> Has it finished starting? Prevents premature liveness kills for slow-starting apps.</li></ul><h4>Probe Types</h4><p>HTTP GET, TCP socket, gRPC, or exec command. Configure initialDelaySeconds, periodSeconds, failureThreshold.</p>',
 mermaid: 'graph LR; PENDING[Pending] --> RUNNING[Running]; RUNNING --> SUCCEEDED[Succeeded]; RUNNING --> FAILED[Failed]; RUNNING --> UNKNOWN[Unknown]; PENDING -.->|scheduling failed| FAILED'
 },
 {
 title: 'Resource Requests, Limits, and QoS Classes',
 content: '<p>Resource management prevents workloads from starving each other and ensures predictable scheduling.</p><h4>Requests vs Limits</h4><ul><li><strong>Requests:</strong> Minimum guaranteed resources. Scheduler uses this for placement decisions. Container is guaranteed this amount.</li><li><strong>Limits:</strong> Maximum resources. Container cannot exceed this. CPU is throttled; memory causes OOMKill.</li></ul><h4>QoS Classes (assigned automatically)</h4><ul><li><strong>Guaranteed:</strong> requests == limits for all containers in Pod. First priority during scheduling. Last to be evicted.</li><li><strong>Burstable:</strong> At least one container has requests set but limits differ from requests. Medium eviction priority.</li><li><strong>BestEffort:</strong> No requests or limits set. First to be evicted under memory pressure. Avoid in production.</li></ul><h4>Resource Types</h4><ul><li><strong>CPU:</strong> Compressible. Measured in millicores (1000m = 1 core). Throttled when exceeding limit.</li><li><strong>Memory:</strong> Incompressible. Measured in Mi/Gi. OOMKilled when exceeding limit.</li><li><strong>Ephemeral storage:</strong> Local disk usage. Evicted when exceeding limit.</li></ul>',
 code: '# Resource configuration example\nspec:\n containers:\n - name: app\n resources:\n requests:\n cpu: 250m # 0.25 cores guaranteed\n memory: 256Mi # 256 MB guaranteed\n limits:\n cpu: 1000m # max 1 core\n memory: 512Mi # OOMKilled if exceeded',
 language: 'yaml'
 },
 {
 title: 'Common Mistakes',
 content: '<p>Workload mistakes that cause production issues:</p><ul><li><strong>No resource limits:</strong> One Pod can consume all node resources, affecting others</li><li><strong>No readiness probes:</strong> Traffic sent to Pods still starting up, causing errors</li><li><strong>Liveness probe too aggressive:</strong> Container killed during temporary load spikes</li><li><strong>Using Deployment for stateful apps:</strong> Data loss when Pods reschedule without stable storage</li><li><strong>Replicas: 1 in production:</strong> Single point of failure, zero availability during updates</li><li><strong>No Pod Disruption Budget:</strong> Cluster upgrades can take down all replicas simultaneously</li><li><strong>BestEffort QoS in production:</strong> Pods evicted first under any memory pressure</li><li><strong>Not setting startupProbe for slow-starting apps:</strong> Liveness probe kills container before it finishes initializing</li></ul>',
 callout: { type: 'warning', title: 'Important', text: 'Always set both resource requests AND limits in production. BestEffort pods are the first eviction targets.' }
 },
 {
 title: 'Interview Tips',
 content: '<p>Key points interviewers want to hear about workloads:</p><ul><li>Deployment vs StatefulSet vs DaemonSet - when to use each</li><li>Rolling update parameters and how zero-downtime deploys work</li><li>QoS classes and eviction order</li><li>Pod lifecycle debugging (why is my Pod in Pending/CrashLoopBackOff?)</li><li>Resource management and capacity planning</li></ul>',
 callout: { type: 'tip', title: 'Pro Tip', text: 'When asked about StatefulSets, always mention the three guarantees: stable network identity, ordered deployment/termination, and persistent storage. Then give a concrete example like a PostgreSQL cluster.' }
 }
 ],
 questions: [
 { id: 'k8s-wl-q1', level: 'junior', title: 'What is a Pod in Kubernetes?', answer: '<p>A Pod is the smallest deployable unit - a group of one or more containers with shared storage and network (same IP address). All containers in a Pod are co-located on the same node and share the same lifecycle. Pods are ephemeral; they can be terminated and replaced at any time.</p>' },
 { id: 'k8s-wl-q2', level: 'junior', title: 'What is the difference between a Deployment and a StatefulSet?', answer: '<p><strong>Deployment:</strong> For stateless apps. Pods are interchangeable, get random names, no stable storage. <strong>StatefulSet:</strong> For stateful apps. Pods get stable names (app-0, app-1), ordered creation/deletion, and persistent volumes that follow them across rescheduling.</p>' },
 { id: 'k8s-wl-q3', level: 'mid', title: 'Explain the three types of probes in Kubernetes and when to use each.', answer: '<p><strong>Liveness:</strong> Is the container alive? Failure restarts it. Use for deadlock detection. <strong>Readiness:</strong> Can it serve traffic? Failure removes from endpoints. Use for dependency checks. <strong>Startup:</strong> Has it finished starting? Disables liveness during startup. Use for slow-starting applications to prevent premature kills.</p>' },
 { id: 'k8s-wl-q4', level: 'mid', title: 'What are the three QoS classes and how are they determined?', answer: '<p><strong>Guaranteed:</strong> All containers have requests == limits (both CPU and memory). Highest priority. <strong>Burstable:</strong> At least one request set but requests != limits. Medium priority. <strong>BestEffort:</strong> No requests or limits set at all. Lowest priority, first evicted. QoS class is assigned automatically based on resource specifications.</p>' },
 { id: 'k8s-wl-q5', level: 'mid', title: 'How does a rolling update work in a Deployment?', answer: '<p>A new ReplicaSet is created with the updated Pod template. Pods in the new RS are gradually scaled up while old RS scales down. maxSurge controls extra Pods allowed; maxUnavailable controls how many can be down. Readiness probes gate the rollout - a Pod must be Ready before the next old Pod is terminated. If a Pod fails to become Ready, the rollout stalls.</p>' },
 { id: 'k8s-wl-q6', level: 'senior', title: 'Explain the sidecar, init container, and ambassador multi-container patterns.', answer: '<p><strong>Sidecar:</strong> Helper running alongside main container (log shipper, Envoy proxy). Shares lifecycle and volumes. <strong>Init container:</strong> Runs to completion BEFORE main containers start. Used for setup (DB migration, config generation, waiting for dependencies). Sequential execution. <strong>Ambassador:</strong> Proxy that simplifies external access. Main connects to localhost; ambassador routes to actual service (connection pooling, auth).</p>' },
 { id: 'k8s-wl-q7', level: 'senior', title: 'When would you use a StatefulSet headless Service and how does DNS work with it?', answer: '<p>A headless Service (clusterIP: None) is required for StatefulSets to enable peer discovery. It creates individual DNS A records for each Pod: pod-name.service-name.namespace.svc.cluster.local. This allows Pods to find each other by stable hostname (e.g., postgres-0.postgres-headless.default.svc.cluster.local). Essential for database replication, leader election, and cluster bootstrapping.</p>' },
 { id: 'k8s-wl-q8', level: 'senior', title: 'A Pod is stuck in CrashLoopBackOff. Walk through your debugging process.', answer: '<p>1) kubectl describe pod - check Events for error messages. 2) kubectl logs pod-name --previous - see logs from crashed container. 3) Check exit code: OOMKilled (code 137) means memory limit hit. 4) Check readiness/liveness probe configuration - too aggressive? 5) Verify image exists and is pullable. 6) Check if init containers completed. 7) Verify environment variables and ConfigMaps/Secrets mounted correctly. 8) Try running container locally with same config.</p>' },
 { id: 'k8s-wl-q9', level: 'lead', title: 'Design a zero-downtime deployment strategy for a database-backed application.', answer: '<p>1) Use rolling updates with maxUnavailable: 0 (no capacity loss). 2) Readiness probes must verify full initialization (DB connection, cache warm). 3) Use Pod Disruption Budgets (minAvailable: 2 for 3 replicas). 4) Database migrations must be backward-compatible (expand/contract pattern). 5) Connection draining via preStop hook (sleep 15s for LB deregistration). 6) Consider canary deployments via progressive delivery (Flagger/Argo Rollouts) for critical services. 7) Automated rollback on error rate spike.</p>' },
 { id: 'k8s-wl-q10', level: 'lead', title: 'How would you implement resource management across multiple teams sharing a cluster?', answer: '<p>1) Namespace per team with ResourceQuotas (max CPU/memory/pods). 2) LimitRange per namespace for default requests/limits (prevent BestEffort). 3) Priority classes for critical vs non-critical workloads. 4) Pod Disruption Budgets for availability guarantees. 5) Cluster autoscaler for dynamic capacity. 6) Cost monitoring/chargeback with tools like Kubecost. 7) Admission webhooks to enforce minimum resource configuration.</p>' },
 { id: 'k8s-wl-q11', level: 'architect', title: 'Compare Deployment, StatefulSet, DaemonSet, and Job. When is each the right choice?', answer: '<p><strong>Deployment:</strong> Stateless services (web servers, APIs). Interchangeable Pods, horizontal scaling. <strong>StatefulSet:</strong> Stateful services (databases, message queues). Need stable identity, ordered operations, persistent volumes. <strong>DaemonSet:</strong> Node-level services (monitoring, logging, networking). One per node. <strong>Job/CronJob:</strong> Batch/scheduled work (backups, reports, migrations). Run to completion, not continuously.</p>' },
 { id: 'k8s-wl-q12', level: 'architect', title: 'Design a multi-tier application deployment with appropriate workload types for each component.', answer: '<p>Web tier: <strong>Deployment</strong> (stateless, HPA on CPU). API tier: <strong>Deployment</strong> with PodAntiAffinity (spread across AZs). Cache: <strong>StatefulSet</strong> (Redis cluster needs stable IDs). Database: <strong>StatefulSet</strong> with volumeClaimTemplates (persistent data). Queue workers: <strong>Deployment</strong> with HPA on queue depth. Monitoring: <strong>DaemonSet</strong> (node metrics). Backups: <strong>CronJob</strong> (nightly DB dump). Migrations: <strong>Job</strong> (pre-deploy hook).</p>' }
 ]
});
