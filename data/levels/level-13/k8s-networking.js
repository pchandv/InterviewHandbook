/* ===================================================================
 KUBERNETES NETWORKING - Level 13: Cloud-Native Engineering
 Pod networking model, Services, Ingress, NetworkPolicies, DNS,
 kube-proxy modes, service mesh integration.
 =================================================================== */
'use strict';
PageData.register('k8s-networking', {
 title: 'Kubernetes Networking',
 level: 13,
 group: 'cloud-native',
 description: 'Master Kubernetes networking: Services, Ingress, NetworkPolicies, DNS, kube-proxy modes, and service mesh patterns.',
 difficulty: 'advanced',
 estimatedMinutes: 55,
 prerequisites: ['k8s-architecture', 'networking-fundamentals'],
 sections: [
 {
 title: 'Introduction',
 content: '<p><strong>Kubernetes networking</strong> follows a flat, routable network model where every Pod gets its own IP address and can communicate with every other Pod without NAT. This simplifies application design but requires understanding the underlying mechanisms.</p><p>This module covers:</p><ul><li>The Pod networking model and CNI plugins</li><li>Services (ClusterIP, NodePort, LoadBalancer, ExternalName)</li><li>Endpoints and EndpointSlices</li><li>kube-proxy modes (iptables, IPVS)</li><li>Ingress controllers and rules</li><li>NetworkPolicies for segmentation</li><li>CoreDNS and service discovery</li><li>Service mesh integration patterns</li></ul><p>Networking is often the most complex aspect of Kubernetes. Mastering it is essential for both operations and interviews.</p>'
 },
 {
 title: 'Pod Networking Model',
 content: '<p>Kubernetes enforces three fundamental networking requirements:</p><ol><li><strong>Pod-to-Pod:</strong> Every Pod can communicate with every other Pod without NAT (even across nodes)</li><li><strong>Node-to-Pod:</strong> Nodes can communicate with all Pods without NAT</li><li><strong>Pod self-awareness:</strong> A Pod sees the same IP that others see for it (no masquerading)</li></ol><h4>How It Works</h4><p>Each node has a CIDR block allocated (e.g., 10.244.1.0/24). Pods on that node get IPs from this range. A <strong>CNI plugin</strong> (Container Network Interface) handles the actual network plumbing:</p><ul><li><strong>Calico:</strong> BGP-based routing or VXLAN overlay. Supports NetworkPolicies natively.</li><li><strong>Cilium:</strong> eBPF-based. High performance, advanced observability, built-in NetworkPolicy.</li><li><strong>Flannel:</strong> Simple VXLAN overlay. No NetworkPolicy support (needs Calico alongside).</li><li><strong>Weave:</strong> Encrypted overlay mesh. Simple setup.</li></ul><h4>Network Namespaces</h4><p>Each Pod gets its own Linux network namespace with a virtual ethernet (veth) pair connecting it to the node bridge. The CNI plugin configures routes between nodes so Pod-to-Pod traffic flows across the cluster.</p>',
 mermaid: 'graph TB; subgraph Node A - 10.244.1.0/24; PA[Pod A - 10.244.1.2] --- BRIDGE1[cbr0 bridge]; PB[Pod B - 10.244.1.3] --- BRIDGE1; end; subgraph Node B - 10.244.2.0/24; PC[Pod C - 10.244.2.2] --- BRIDGE2[cbr0 bridge]; PD[Pod D - 10.244.2.3] --- BRIDGE2; end; BRIDGE1 -->|overlay/BGP| BRIDGE2; PA -->|direct IP| PC'
 },
 {
 title: 'Services',
 content: '<p>A <strong>Service</strong> provides a stable network endpoint for a dynamic set of Pods. Pods come and go, but the Service IP and DNS name remain constant.</p><h4>ClusterIP (default)</h4><p>Internal-only virtual IP. Accessible only within the cluster. kube-proxy programs iptables/IPVS rules to load-balance traffic across backend Pods.</p><h4>NodePort</h4><p>Exposes Service on a static port (30000-32767) on every node. External traffic can reach the Service via NodeIP:NodePort. Builds on ClusterIP.</p><h4>LoadBalancer</h4><p>Provisions a cloud load balancer (AWS ELB/NLB, GCP LB, Azure LB) that routes to NodePorts. The standard way to expose services externally in cloud environments.</p><h4>ExternalName</h4><p>Maps a Service to an external DNS name (CNAME). No proxying. Useful for referencing external services (RDS, external APIs) with a cluster-local DNS name.</p><h4>How Services Work</h4><p>Services use label selectors to identify backend Pods. The Endpoints (or EndpointSlices) controller watches for Pods matching the selector and maintains the list of healthy endpoints. kube-proxy watches these and updates routing rules.</p>',
 code: '# ClusterIP Service\napiVersion: v1\nkind: Service\nmetadata:\n name: web-service\nspec:\n type: ClusterIP\n selector:\n app: web\n ports:\n - port: 80\n targetPort: 8080\n protocol: TCP',
 language: 'yaml'
 },
 {
 title: 'Endpoints and kube-proxy Modes',
 content: '<p><strong>Endpoints</strong> track the IP:port pairs of Pods backing a Service. <strong>EndpointSlices</strong> (Kubernetes 1.21+) are the scalable replacement - split into chunks of 100 endpoints for better performance in large clusters.</p><h4>kube-proxy iptables Mode</h4><p>Default mode. Creates iptables rules for each Service/endpoint pair. Pros: proven, reliable. Cons: O(n) rule matching, slow with 10,000+ services, random load balancing only, full rule rebuild on any change.</p><h4>kube-proxy IPVS Mode</h4><p>Uses Linux kernel IPVS (IP Virtual Server). Pros: O(1) hash-based lookup, handles 100K+ services efficiently, multiple LB algorithms (round-robin, least-connections, source-hash). Cons: requires kernel IPVS modules, slightly more complex debugging.</p><h4>When to Switch to IPVS</h4><ul><li>Cluster has 1,000+ Services</li><li>Need session affinity beyond clientIP timeout</li><li>Need weighted load balancing</li><li>Experiencing high latency from iptables rule evaluation</li></ul>',
 table: {
 headers: ['Feature', 'iptables', 'IPVS'],
 rows: [
 ['Lookup Time', 'O(n) linear', 'O(1) hash table'],
 ['Max Services', '~5,000 practical', '100,000+'],
 ['LB Algorithms', 'Random only', 'RR, LC, weighted, source-hash'],
 ['Rule Updates', 'Full rebuild', 'Incremental'],
 ['Debugging', 'iptables -L -t nat', 'ipvsadm -Ln']
 ]
 }
 },
 {
 title: 'Ingress',
 content: '<p><strong>Ingress</strong> manages external HTTP/HTTPS access to Services. It provides host-based and path-based routing, TLS termination, and name-based virtual hosting - all from a single external IP.</p><h4>Ingress Components</h4><ul><li><strong>Ingress Resource:</strong> Declarative rules (host, path, backend Service)</li><li><strong>Ingress Controller:</strong> Implements the rules (NGINX, Traefik, HAProxy, AWS ALB, GCE)</li></ul><h4>Routing Types</h4><ul><li><strong>Path-based:</strong> /api goes to api-service, /web goes to web-service</li><li><strong>Host-based:</strong> api.example.com goes to api-service, www.example.com goes to web-service</li><li><strong>Combined:</strong> Both host and path rules together</li></ul><h4>TLS Termination</h4><p>Ingress handles TLS at the edge. Certificate stored in a Secret, referenced in Ingress spec. Backend connections can be HTTP (common) or re-encrypted (secure environments).</p><h4>Popular Ingress Controllers</h4><ul><li><strong>NGINX Ingress:</strong> Most widely used. Rich annotations for rate limiting, rewrites, auth.</li><li><strong>Traefik:</strong> Auto-discovery, Lets Encrypt integration, middleware chains.</li><li><strong>AWS ALB Controller:</strong> Provisions actual ALBs per Ingress in AWS.</li><li><strong>Istio Gateway:</strong> Part of Istio service mesh. Advanced traffic management.</li></ul>',
 code: 'apiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n name: app-ingress\n annotations:\n nginx.ingress.kubernetes.io/rewrite-target: /\nspec:\n ingressClassName: nginx\n tls:\n - hosts:\n - app.example.com\n secretName: app-tls-secret\n rules:\n - host: app.example.com\n http:\n paths:\n - path: /api\n pathType: Prefix\n backend:\n service:\n name: api-service\n port:\n number: 80\n - path: /\n pathType: Prefix\n backend:\n service:\n name: web-service\n port:\n number: 80',
 language: 'yaml'
 },
 {
 title: 'NetworkPolicies',
 content: '<p><strong>NetworkPolicies</strong> control traffic flow between Pods at the IP/port level. Without them, all Pods can communicate freely (allow-all default). They are the Kubernetes equivalent of firewall rules.</p><h4>Key Concepts</h4><ul><li>Policies are <strong>namespaced</strong> - they apply to Pods in their namespace</li><li>Policies are <strong>additive</strong> - multiple policies combine (union of allowed traffic)</li><li>Once ANY policy selects a Pod, all non-explicitly-allowed traffic is denied (default-deny effect)</li><li><strong>Ingress rules:</strong> Control incoming traffic TO selected Pods</li><li><strong>Egress rules:</strong> Control outgoing traffic FROM selected Pods</li></ul><h4>Selector Types</h4><ul><li><strong>podSelector:</strong> Match Pods by labels in same namespace</li><li><strong>namespaceSelector:</strong> Match all Pods in namespaces with certain labels</li><li><strong>ipBlock:</strong> Match CIDR ranges (for external services)</li></ul><h4>CNI Requirements</h4><p>NetworkPolicies require a CNI plugin that supports them. Calico, Cilium, and Weave support them. Flannel alone does NOT - you need Flannel + Calico (Canal).</p>',
 code: '# Deny all ingress, allow only from frontend\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n name: api-allow-frontend\nspec:\n podSelector:\n matchLabels:\n app: api\n policyTypes: [Ingress]\n ingress:\n - from:\n - podSelector:\n matchLabels:\n app: frontend\n ports:\n - protocol: TCP\n port: 8080',
 language: 'yaml'
 },
 {
 title: 'CoreDNS and Service Discovery',
 content: '<p><strong>CoreDNS</strong> is the cluster DNS server. Every Service and Pod gets a DNS record automatically, enabling name-based service discovery.</p><h4>DNS Records</h4><ul><li><strong>Service:</strong> service-name.namespace.svc.cluster.local resolves to ClusterIP</li><li><strong>Headless Service:</strong> Resolves to individual Pod IPs (A records per Pod)</li><li><strong>Pod:</strong> pod-ip-dashed.namespace.pod.cluster.local (rarely used directly)</li><li><strong>SRV records:</strong> Include port information for named ports</li></ul><h4>DNS Resolution in Pods</h4><p>Pods have /etc/resolv.conf configured to point to CoreDNS ClusterIP. Default search domains allow short names: service-name resolves within same namespace, service-name.other-namespace for cross-namespace.</p><h4>dnsPolicy Options</h4><ul><li><strong>ClusterFirst (default):</strong> Cluster DNS for cluster domains, upstream for external</li><li><strong>Default:</strong> Inherits node DNS config (skips cluster DNS)</li><li><strong>None:</strong> Custom DNS config via dnsConfig field</li></ul><h4>CoreDNS Plugins</h4><p>CoreDNS is plugin-based. Key plugins: kubernetes (cluster records), forward (upstream resolution), cache (performance), loop detection, health/ready endpoints.</p>'
 },
 {
 title: 'Service Mesh Integration',
 content: '<p>A <strong>service mesh</strong> adds observability, traffic management, and security to service-to-service communication without code changes.</p><h4>How It Works</h4><p>Sidecar proxies (typically Envoy) are injected into every Pod. All traffic flows through the proxy, which handles mTLS encryption, retries, circuit breaking, and telemetry collection.</p><h4>Popular Service Meshes</h4><ul><li><strong>Istio:</strong> Most feature-rich. Control plane (istiod) + Envoy sidecars. Complex but powerful.</li><li><strong>Linkerd:</strong> Lightweight, Rust-based proxy. Simpler operations, lower resource overhead.</li><li><strong>Cilium Service Mesh:</strong> eBPF-based, sidecar-less. Kernel-level processing for high performance.</li></ul><h4>Key Capabilities</h4><ul><li><strong>mTLS:</strong> Automatic encryption and identity between all services</li><li><strong>Traffic splitting:</strong> Canary deployments (90/10 traffic split)</li><li><strong>Observability:</strong> Distributed tracing, golden metrics (latency, traffic, errors, saturation)</li><li><strong>Circuit breaking:</strong> Prevent cascade failures</li><li><strong>Rate limiting:</strong> Per-service traffic control</li></ul>',
 mermaid: 'graph LR; subgraph Pod A; APP_A[App Container] --> PROXY_A[Envoy Sidecar]; end; subgraph Pod B; PROXY_B[Envoy Sidecar] --> APP_B[App Container]; end; PROXY_A -->|mTLS| PROXY_B; CTRL[istiod Control Plane] -.->|config/certs| PROXY_A; CTRL -.->|config/certs| PROXY_B'
 },
 {
 title: 'Common Mistakes',
 content: '<p>Networking mistakes that cause connectivity issues and security vulnerabilities:</p><ul><li><strong>No NetworkPolicies:</strong> All Pods can talk to all Pods by default - a compromised Pod has lateral movement to entire cluster</li><li><strong>Using NodePort in production:</strong> Exposes ports on ALL nodes, bypasses load balancer health checks. Use LoadBalancer or Ingress instead.</li><li><strong>Ignoring DNS caching:</strong> Applications that resolve DNS once and cache forever miss Service endpoint changes. Use short TTLs or connection pools that re-resolve.</li><li><strong>Not understanding headless Services:</strong> Regular Services give you load balancing; headless gives you all Pod IPs. Using wrong type causes unexpected behavior with StatefulSets.</li><li><strong>Ingress without rate limiting:</strong> Single client can overwhelm backend services. Always configure rate limiting annotations.</li><li><strong>Missing egress NetworkPolicies:</strong> Even if ingress is locked down, a compromised Pod can still make outbound calls to external command-and-control servers.</li></ul>',
 callout: { type: 'warning', title: 'Security', text: 'Always implement a default-deny NetworkPolicy in production namespaces, then explicitly allow required traffic flows.' }
 },
 {
 title: 'Interview Tips',
 content: '<p>What interviewers expect for Kubernetes networking:</p><ul><li>Explain the flat networking model and why Kubernetes chose it</li><li>Compare Service types and when to use each</li><li>How does traffic flow from external user to a Pod? (LB -> NodePort -> ClusterIP -> Pod)</li><li>How would you implement network segmentation in a multi-tenant cluster?</li><li>Ingress vs LoadBalancer Service - trade-offs</li><li>Service mesh benefits and when the complexity is justified</li></ul>',
 callout: { type: 'tip', title: 'Pro Tip', text: 'When discussing Services, draw the traffic flow: External -> LoadBalancer -> NodePort -> ClusterIP -> Endpoints -> Pod. Show you understand each layer.' }
 }
 ],
 questions: [
 { id: 'k8s-net-q1', level: 'junior', title: 'What are the four types of Kubernetes Services?', answer: '<p><strong>ClusterIP:</strong> Internal-only virtual IP (default). <strong>NodePort:</strong> Exposes on static port (30000-32767) on every node. <strong>LoadBalancer:</strong> Provisions cloud LB pointing to NodePorts. <strong>ExternalName:</strong> DNS CNAME to external service, no proxying.</p>' },
 { id: 'k8s-net-q2', level: 'junior', title: 'How does DNS-based service discovery work in Kubernetes?', answer: '<p>CoreDNS runs as a cluster add-on. Every Service gets a DNS record: service-name.namespace.svc.cluster.local. Pods resolve short names within their namespace (just the service name works). Pod /etc/resolv.conf points to CoreDNS ClusterIP. Applications connect by name; DNS returns the Service ClusterIP.</p>' },
 { id: 'k8s-net-q3', level: 'mid', title: 'Explain how a packet travels from an external user to a Pod behind a LoadBalancer Service.', answer: '<p>1) User hits cloud LB public IP. 2) LB health-checks NodePort on all nodes, forwards to healthy node. 3) Traffic hits NodePort on the node. 4) kube-proxy iptables/IPVS rules DNAT to a backend Pod IP (may be on same or different node). 5) If different node, packet is routed via overlay/BGP. 6) Packet arrives at Pod network namespace. Response follows reverse path with SNAT.</p>' },
 { id: 'k8s-net-q4', level: 'mid', title: 'What is a NetworkPolicy and what happens without one?', answer: '<p>A NetworkPolicy controls Pod-to-Pod traffic at IP/port level. Without any policies, all Pods can communicate freely (allow-all default). Once any policy selects a Pod, non-allowed traffic is denied. Policies are additive (union of rules). They require a supporting CNI plugin (Calico, Cilium). Flannel alone does NOT enforce them.</p>' },
 { id: 'k8s-net-q5', level: 'mid', title: 'What is the difference between Ingress and a LoadBalancer Service?', answer: '<p><strong>LoadBalancer Service:</strong> One cloud LB per Service. Layer 4 (TCP/UDP). Expensive with many services. Simple setup. <strong>Ingress:</strong> Single LB shared by many Services. Layer 7 (HTTP/HTTPS). Host/path-based routing. TLS termination. More cost-effective for HTTP workloads. Requires Ingress Controller deployment.</p>' },
 { id: 'k8s-net-q6', level: 'senior', title: 'Compare iptables and IPVS kube-proxy modes. When would you switch to IPVS?', answer: '<p><strong>iptables:</strong> Default. O(n) rule matching, practical limit ~5000 services, random LB, full rule rebuild on changes. <strong>IPVS:</strong> O(1) hash lookup, handles 100K+ services, supports round-robin/least-connections/weighted/source-hash, incremental updates. Switch to IPVS when: 1000+ services, need non-random LB, experiencing high iptables rule evaluation latency, or need session affinity beyond simple timeout.</p>' },
 { id: 'k8s-net-q7', level: 'senior', title: 'How would you implement network segmentation in a multi-tenant Kubernetes cluster?', answer: '<p>1) Namespace per tenant. 2) Default-deny NetworkPolicy in each namespace. 3) Explicit allow rules only for required flows (ingress from Ingress controller, egress to required services). 4) Namespace labels for cross-namespace policies. 5) Separate Ingress classes per tenant if needed. 6) Consider Cilium ClusterWide NetworkPolicies for global rules. 7) Egress policies to prevent data exfiltration. 8) Audit policies regularly with tools like Illuminatio or Netfetch.</p>' },
 { id: 'k8s-net-q8', level: 'senior', title: 'Explain how Kubernetes Ingress handles TLS termination and what security considerations apply.', answer: '<p>TLS cert and key stored in a Kubernetes Secret. Ingress references the Secret in its tls spec. The Ingress Controller (NGINX, Traefik) terminates TLS at the edge, forwarding plain HTTP to backend Services. Security considerations: cert rotation (cert-manager automates this), redirect HTTP to HTTPS, HSTS headers, minimum TLS version (1.2+), backend re-encryption for sensitive workloads, separate certs per domain.</p>' },
 { id: 'k8s-net-q9', level: 'lead', title: 'Design a networking architecture for a production Kubernetes cluster hosting multiple microservices with external and internal APIs.', answer: '<p>1) CNI: Cilium (eBPF performance + NetworkPolicy + observability). 2) External traffic: Single NGINX Ingress Controller with WAF annotations. 3) Internal APIs: ClusterIP Services only, never exposed externally. 4) NetworkPolicies: Default-deny per namespace, explicit allow between services. 5) DNS: CoreDNS with appropriate cache TTLs. 6) mTLS: Service mesh (Istio/Linkerd) for inter-service encryption. 7) Egress: Controlled egress with NetworkPolicies + egress gateways for auditing. 8) Monitoring: Hubble (Cilium) for network flow visibility.</p>' },
 { id: 'k8s-net-q10', level: 'lead', title: 'A Service is not reachable from within the cluster. Walk through your debugging process.', answer: '<p>1) Verify Service exists and has correct selector: kubectl get svc, kubectl describe svc. 2) Check Endpoints: kubectl get endpoints - empty means no matching Pods or Pods not Ready. 3) Verify Pod labels match Service selector. 4) Check Pod readiness probes passing. 5) Test from within cluster: kubectl exec curl-pod -- curl service-name:port. 6) Check kube-proxy is running on nodes: check logs, iptables rules. 7) Verify CoreDNS is healthy: nslookup service-name from Pod. 8) Check NetworkPolicies not blocking traffic. 9) Verify CNI plugin health on affected nodes.</p>' },
 { id: 'k8s-net-q11', level: 'architect', title: 'When is a service mesh justified and when is it over-engineering?', answer: '<p><strong>Justified:</strong> 50+ microservices with complex communication patterns, strict compliance requiring mTLS everywhere, need for traffic splitting (canary/blue-green), distributed tracing requirements, multiple teams needing consistent security without code changes. <strong>Over-engineering:</strong> Less than 10 services, simple request patterns, team unfamiliar with operational complexity, resource-constrained cluster (sidecars add ~50MB RAM per Pod), latency-sensitive workloads where sidecar hop matters.</p>' },
 { id: 'k8s-net-q12', level: 'architect', title: 'Design a multi-cluster networking solution for a globally distributed application.', answer: '<p>1) Multi-cluster service mesh (Istio multi-cluster or Linkerd multicluster) for cross-cluster service discovery. 2) Global load balancer (GCP Global LB, AWS Global Accelerator) for anycast-based user routing. 3) Each cluster has own Ingress with regional certs. 4) Cross-cluster traffic via encrypted tunnels or direct peering (VPC peering, Transit Gateway). 5) Federated CoreDNS or external service discovery (Consul). 6) NetworkPolicies enforced consistently via policy-as-code. 7) Centralized network observability (Hubble UI, Kiali). 8) Egress gateways per cluster for auditable external access.</p>' }
 ]
});
